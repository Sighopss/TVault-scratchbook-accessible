---
inclusion: fileMatch
fileMatchPattern: 'sdk/**,demo-app/**,infra/**,scripts/**,.github/**,Makefile'
---

Trevor lane. Load `skills/trevor-recorder/SKILL.md` first, then only the sibling listed in its table. Do not edit `vault/` or `web/`. Map: `README.md` then `PLAN.md` then `skills/INDEX.md`.
