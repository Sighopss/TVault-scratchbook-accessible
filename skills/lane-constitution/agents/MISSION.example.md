# Mission: `<your-name>-<id>`

**Id:** `<your-name>-<id>`  
**Write:** `<paths>` only  
**Read:** `PLAN.md`, `skills/<your-lane>/SKILL.md`, this file, `contracts/` if present.

You are implementing **this mission**. You are not the rest of the lane. You are not another human’s slice.

## Goal

One paragraph. Your outcome, not the whole product.

## Files to create

```
<paths>
```

## Behavior

Public API / UI contract this mission owns. Inputs, outputs, failure modes.

## Tests

Named tests. What must pass. What must fail closed.

## Never

- Edit another mission’s paths.
- Edit another human’s paths.
- Invent files, APIs, or stack not in this mission and `stack.md`.

Copy this file to `agents/<id>.md` and delete the `.example` copy when the mission is real.
