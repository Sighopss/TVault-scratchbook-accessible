# Contracts

Canonical HTTP + auth is **PLAN.md**. Hour 0 in the product repo:

| Copy from | Becomes |
|---|---|
| PLAN.md section **HTTP + auth** (or this folder’s `http.draft.md` pointer) | `contracts/http.md` |
| `skills/trevor-recorder/span.schema.draft.json` | `contracts/span.schema.json` |
| Full flight fixtures (not single spans) | `contracts/fixtures/tenant-a-rag.json`, `contracts/fixtures/tenant-b-forbidden.json` |

Locked hour 0. The product repo's `contracts/http.md` carries these filenames — read them there, not here.
